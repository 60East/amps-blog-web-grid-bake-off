// Here are the main project controls
const CLIENT_NAME = 'quick-start-demo';
const TRANSPORT = 'ws';
const HOST = 'localhost';
const PROTOCOL = 'amps';
const PORT = '9000';
const URI = TRANSPORT + '://' + HOST + ':' + PORT + '/' + PROTOCOL + '/';


module.exports = {CLIENT_NAME: CLIENT_NAME, TRANSPORT: TRANSPORT, HOST: HOST, PROTOCOL: PROTOCOL, PORT: PORT, URI: URI};
